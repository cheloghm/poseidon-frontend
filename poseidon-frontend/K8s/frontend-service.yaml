apiVersion: v1
kind: Service
metadata:
  name: poseidon-frontend-service
spec:
  selector:
    app: poseidon-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer
